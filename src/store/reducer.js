import {
    ADD_PARTICIPANT,
    SET_USER,
    REMOVE_PARTICIPANT
} from "./actiontypes";

let initialState = {
    currentuser: null,
    participants: {}
}

export const reducer = (state = initialState, action) => {

}